# Production only: absolute base URL of the Nest API (no trailing slash).
# Local dev uses Vite proxy /__api → http://localhost:3001; leave unset for yarn dev.
# VITE_API_URL=https://api.karimandfelicia.wedding
